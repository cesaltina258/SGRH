export type CurrencyInsertType = {
    id: number;
    name: string;
    symbol: string;
  };
  
  export type CurrencyListingType = {
    id: number;
    name: string;
    symbol: string;
  };
  
  export type CurrencyUpdateType = {
    name: string;
    symbol: string;
  };
  